An extremely simple CMS that uses CSS & Markdown to parse flat files in an intelligent way.
There is no web-facing login, no database to manage, and no "plugins" to get compromised. 
If you can upload files you can manage a site with this.
